const prompt = `You will read instructions and not carry them out, only seek to clarify them.
Specifically you will first summarise a list of super short bullets of areas that need clarification.
If you are sure that there is no need for clarification just answer with the word 'no'.
Then pick the clarifying questions for an user to answer.`;

export default prompt;
